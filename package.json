{
  "name": "jsrepos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.js --bundle --platform=node --outfile=distro/index.ts",
    "start": "node ./distro/index.ts",
    "dev": "node build.js -watch -run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.20",
    "esbuild": "^0.8.29",
    "estrella": "^1.3.0"
  },
  "dependencies": {
    "primes": "0.0.1"
  }
}
